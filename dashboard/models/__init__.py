"""Persistence layer (design §3.0.1 rule 1, §4.1–§4.5).

Empty by design at task 1.1. Models are added by tasks 1.3 (``operators``) and
2.1–2.3 (``leads``, ``deals``, and the remaining tables of Requirement 13.5),
and re-exported here so ``from dashboard.models import X`` is the single import
surface the import-linter contracts target.
"""
