"""Settings package.

`base.py` holds the shared configuration. Task 1.2 adds the five deployment
keys of design §3.0.3 here; environment-specific overlays (if any) become
sibling modules that start with ``from .base import *``.
"""
