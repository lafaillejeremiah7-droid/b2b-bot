"""Settings package.

`base.py` holds the shared configuration, including the five deployment keys of
design §3.0.3; environment-specific overlays (if any) become sibling modules
that start with ``from .base import *``.
"""
